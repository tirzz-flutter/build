name: Web2APK Build

# Dipicu oleh server.js (triggerWeb2ApkWorkflow) lewat workflow_dispatch.
on:
  workflow_dispatch:
    inputs:
      web_url:
        description: "URL website yang akan dibungkus jadi APK"
        required: true
        type: string
      app_name:
        description: "Nama aplikasi"
        required: true
        type: string
      icon_url:
        description: "URL icon aplikasi (PNG), opsional"
        required: false
        type: string
      tag:
        description: "Tag unik build"
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.19.6"
          channel: "stable"
          cache: true

      - name: Buat skeleton project Flutter WebView
        run: |
          set -e
          flutter create --platforms=android --project-name web2apk_app web2apk_app
          cd web2apk_app

          cat >> pubspec.yaml << 'EOF'
            webview_flutter: ^4.7.0
          EOF

      - name: Tulis main.dart (WebView wrapper)
        working-directory: web2apk_app
        run: |
          set -e
          cat > lib/main.dart << EOF
          import 'package:flutter/material.dart';
          import 'package:webview_flutter/webview_flutter.dart';

          void main() => runApp(const Web2ApkApp());

          class Web2ApkApp extends StatelessWidget {
            const Web2ApkApp({super.key});
            @override
            Widget build(BuildContext context) {
              return MaterialApp(
                title: '${{ inputs.app_name }}',
                debugShowCheckedModeBanner: false,
                home: const WebViewScreen(),
              );
            }
          }

          class WebViewScreen extends StatefulWidget {
            const WebViewScreen({super.key});
            @override
            State<WebViewScreen> createState() => _WebViewScreenState();
          }

          class _WebViewScreenState extends State<WebViewScreen> {
            late final WebViewController controller;

            @override
            void initState() {
              super.initState();
              controller = WebViewController()
                ..setJavaScriptMode(JavaScriptMode.unrestricted)
                ..loadRequest(Uri.parse('${{ inputs.web_url }}'));
            }

            @override
            Widget build(BuildContext context) {
              return Scaffold(
                body: SafeArea(child: WebViewWidget(controller: controller)),
              );
            }
          }
          EOF

      - name: Ganti nama aplikasi di AndroidManifest
        working-directory: web2apk_app
        run: |
          set -e
          sed -i "s/android:label=\"web2apk_app\"/android:label=\"${{ inputs.app_name }}\"/" android/app/src/main/AndroidManifest.xml || true

      - name: Download & pasang icon (jika ada)
        if: ${{ inputs.icon_url != '' }}
        working-directory: web2apk_app
        run: |
          set -e
          mkdir -p assets
          curl -sSL -o assets/icon.png "${{ inputs.icon_url }}"
          cat >> pubspec.yaml << 'EOF'
            flutter_launcher_icons: ^0.13.1
          EOF
          cat > flutter_launcher_icons.yaml << 'EOF'
          flutter_launcher_icons:
            android: true
            ios: false
            image_path: "assets/icon.png"
          EOF

      - name: Flutter pub get
        working-directory: web2apk_app
        run: flutter pub get

      - name: Generate launcher icons
        if: ${{ inputs.icon_url != '' }}
        working-directory: web2apk_app
        run: dart run flutter_launcher_icons

      - name: Build APK (release)
        working-directory: web2apk_app
        run: flutter build apk --release

      - name: Siapkan output APK
        run: |
          set -e
          mkdir -p output
          cp web2apk_app/build/app/outputs/flutter-apk/app-release.apk "output/${{ inputs.tag }}.apk"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: apk-build-${{ inputs.tag }}
          path: output/*.apk
          retention-days: 1
